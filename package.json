{
  "name": "textbook-registry",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "validate": "node scripts/validate.mjs",
    "check:github": "node scripts/check-github.mjs",
    "parity": "node parity/parity.mjs"
  },
  "devDependencies": {
    "ajv": "8.20.0"
  }
}
